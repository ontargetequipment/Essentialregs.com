**Claude Code settings for this prompt — Model: Fable · Effort: high** (code change + investigation)

# Claude Code prompt — make the `neighbors_only` embed rebuild survive a 32,000-provision corpus

Context: the Batch 5 import (run on main at `35d4cad`) succeeded through the re-link step, but the final Embed workflow with `neighbors_only` failed with `canceling statement due to statement timeout (57014)` after ~19,000 of 32,053 provisions (batch starting at `sec-gp09-IX-C`, 3 retries exhausted). The corpus was 29,477 provisions at the last successful full rebuild; it is 32,054 now and will keep growing (Batch 6 adds ~3,500 more next week, then OSHA and DOT add tens of thousands). A plain re-run is not acceptable; the job must scale. **Owner-approved spend for this prompt: one `neighbors_only` rebuild's worth of database time. No summaries, no new embeddings (Voyage) — the provision embeddings already exist.**

1. **Read `pipeline/embed.py`** (the neighbour-rebuild path used by `neighbors_only`) and `pipeline/test_embed.py`, and the Embed workflow. Report: how the neighbour query is issued (RPC / SQL via service role / PostgREST), the batch size, what the retry loop does, and where the time goes (is it one big `ORDER BY embedding <=> …` per batch, a delete + insert per batch, or a whole-table join?). Check the Supabase project's statement timeout for the role the job uses (`show statement_timeout` via the same client) and whether `provision_embeddings` has an HNSW/IVFFlat index on the embedding column (query `pg_indexes`). Paste the findings before changing anything.

2. **Fix so the job completes and stays completable as the corpus grows.** Requirements: (a) batches sized so each statement finishes well inside the timeout — an adaptive loop that halves the batch on a `57014` and continues (the p195 import's upsert already does this for row upserts; mirror that pattern), never exhausting retries on a size that could still be halved; (b) **resumable** — a `--start-after <provision id>` (or equivalent checkpoint) so a rerun does not redo the first 19,000 rows, and the workflow exposes it as an input; (c) progress logging every N batches with the current id so a failure is diagnosable; (d) if the per-batch query lacks an ANN index and step 1 shows one is missing, add a migration file `supabase/migrations/RUN_ME_<n>_embedding_index.sql` creating an HNSW index (`vector_cosine_ops`) with a comment on expected build time — **do not apply it yourself** (migrations are the owner's to run); say clearly in the report whether the job completes without it. Tests for the adaptive halving and the resume point in `test_embed.py`; all four pipeline suites green.

3. **Commit and push** the fix. Then run Embed `neighbors_only` (resuming from `sec-gp09-IX-C` if the resume input works, otherwise from the start) and let it finish. Paste: final batch size the loop settled at, wall time, provisions and neighbour-row counts, and confirm `select count(*) from provisions p where not exists (select 1 from provision_neighbors n where n.provision_id = p.id)` is 0 (or list the ids if not).

4. **Freshness.** Trigger the freshness workflow. Expect the four new `sos` rows (5 CCR 1001-13 / 1001-15 / 1001-29 / 1001-31, ruleVersionIds 12430 / 11881 / 12376 / 11838) unchanged and "No changes detected." Paste the last few lines.

5. Do **not** change the Reg 25 `I.L.1.c.(xv)` duplicate — it is a genuine source-text duplicate (two definitions printed under one label); the merged row is the intended handling for now and the summary hint covers both terms.

Report each step's result in order, then stop.
