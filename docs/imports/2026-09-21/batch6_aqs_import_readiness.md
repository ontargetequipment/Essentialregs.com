# `aqs` — Air Quality Standards, Designations and Emission Budgets (5 CCR 1001-14) — import readiness report

## Verdict: READY WITH NOTES

The document parses to **80 rows** (root + 8 top-level sections + 71 items); every gate passes; both tables come through as real `<table>`s; the nine area maps get figure placeholders; the 35 statement-of-basis entries are one row each, in order; 0 orphans / 0 duplicate ids / 0 dead cross-reference targets; and the five existing parses (Reg 26, 7, 30, ECMC, 25) are **byte-identical** with `aqs` held out of the corpus — with it in, only Reg 7 changes, by exactly two new anchors (listed below). The notes: (1) `III.A.` is the printed line "III.A. through III.E. Repealed" (III.B–E do not exist, as printed) and that row also carries the Section III classification table printed after it; (2) one pdfplumber cell reads "Attainment/Mainte nance" (a hyphen-less line break inside a PDF table cell); (3) the V.A.1. budgets table's header row is config-supplied (the source prints none); (4) the printed citation typo "Section V.a.1." (lower-case a, in V.A.4.j.) is left as printed and lands in the `unparseable` bucket.

## Regulation identity
- Title as printed (REG_AQS.txt lines 15-16, PDF title page): **AIR QUALITY STANDARDS, DESIGNATIONS AND EMISSION BUDGETS**, then **5 CCR 1001-14** on its own line → `root_title` = `AIR QUALITY STANDARDS, DESIGNATIONS AND EMISSION BUDGETS 5 CCR 1001-14` (exact); `root_citation` = `Code of Colorado Regulations · Air Quality Standards, Designations and Emission Budgets`; key `aqs`; root id `sec-aqs-top-REG-aqs`.
- Effective 01/14/2026 (SOS ruleVersionId 12357, ruleId 2347 — recorded in `sources/manifest.json` as a `sos` entry, same shape as Reg 1); 82 PDF pages, 3,427 text lines. Editor's Notes (page 82) end "Rules V.A.1, VIII.II eff. 01/14/2026."
- Parts found: **none** (`no_parts: True`, like Reg 1 / cp / 9 — `grep "^\s*PART [A-Z]" sources/REG_AQS.txt` is empty). Top level = 8 roman sections I–VIII, every label printed as the full dotted compound, so ids are `sec-aqs-I-B-1`, `sec-aqs-V-A-4-e`, `sec-aqs-VIII-DD`.
- No appendices. Nine full-page map images (PDF pages 5–13, one per III.F–III.N heading) — pdfplumber reports exactly one image on each of those pages and none elsewhere.

## Row counts
- Total **80**: root 1; by section — I 4 (I., I.A., I.B., I.B.1.), II 1 ("II. Reserved"), III 11 (III., III.A., III.F.–III.N.), IV 1, V 21 (V., V.A., V.A.1.–4., V.A.4.a.–j., V.B., V.C., V.C.1.–2., V.D.), VI 1, VII 4, VIII 36 (heading + 35 entries A–Z, AA–II). Kinds: root 1 / section 8 / item 71; 20 heading-only rows (Reserved/Repealed rows, the nine map rows, short headings).
- Rows with ≥ 25 words (summary-eligible): **54** (VIII 35, V 10, III 3, VII 3, I/IV/VI one each) ≈ $0.16 at $0.30/100.
- Longest row: `sec-aqs-VIII-G` (Colorado Springs CO redesignation statement, January 15, 1998) — 10,522 chars / 1,606 words; then VIII.Z 8,776, VII.A 8,489, VIII.A 7,608. Nothing over 12,000 chars.
- Coverage: 23,373 words in the cleaned body (from "I. Ambient Air Quality Standards" to the end, furniture stripped) vs 23,502 across all `full_text` (100.6% — the surplus is label text and the config-supplied table header).

## Changes made to `import_ccr.py` (each with the reason)
1. `CORPUS_REGS["aqs"] = "aqs"`; `REG_META["aqs"]` — `no_parts`, state / CDPHE-APCD / AQCC URL, the exact root citation/title above, plus the new flag `heading_line_own_paragraph` (item 8).
2. `SOB_SECTION_CONFIG["aqs"] = {"": "VIII"}` — the statement of basis is the last top-level SECTION (Reg 8's per-section shape, keyed by `NO_PART` since the document is part-less); its entries are ordinary compound labels with a topic (not a date) on the label line and "Adopted: <date>" as the next paragraph (the Reg 12 Part D shape). Once "VIII." is open only VIII-rooted, ≥ 2-token labels are structure, which keeps the entries' flush-left "I. Existing data…" / "II. The Commission concluded…" finding lists as body text (a mini-parse test shows that with Reg 1's config those same lines become spurious sections). No `SOB_PART_CONFIG` entry, no new SOB family.
3. `KNOWN_LABEL_FIXES["aqs"]` — 9 entries, see "Label fixes" below.
4. `KNOWN_TEXT_FIXES["aqs"]` — 20 entries: the superscript footnote markers of I.B.1. (SO2) and IV (visibility) rewritten as `[1]`…`[5]` on both the reference and the footnote text (pdftotext glued them to the preceding word — "(SO2)1", "32 miles2", "standard.3" — and dropped each footnote's own digit onto a line of its own); the six orphaned single-digit lines removed (each pinned by the exact preceding line); the "of.076/km" / "of.01/km" dropped spaces restored; and the Editor's Notes 70-underscore rule line removed (it opens page 82 directly under VIII.II's last sentence — the Reg 11 Appendix B shape). All 29 fixes report `OK` (1 hit).
5. `UNCAPTIONED_TABLES["aqs"]` — the Section III Area / Classification / Boundary table (pages 3–4, `spans=[(3,0),(4,0)]`, the page-4 header reprint dropped, `stop_prefix` = the "* The classification of the Denver Metro Area…" footnote, which is printed with no blank line after the last row). Existing mechanism, no code change.
6. **New** `COLUMN_LAYOUT_TABLES` + `_parse_column_layout_table` / `_swap_column_layout_tables` — the V.A.1. motor-vehicle-emissions-budget table (pages 15–19) is borderless (pdfplumber finds no table) with cells wrapped over up to 30 physical lines and rows that straddle page breaks, so neither `LAYOUT_TEXT_TABLES` (one physical line = one row) nor `UNCAPTIONED_TABLES` fits. Rows = blank-line-delimited runs of the `-layout` dump (a page seam inside a row leaves no blank because `clean_pages` strips the furniture); cells = 2+-space-separated fragments assigned by character offset to `col_starts=[0, 30, 70]` (the printed column starts drift 34–43 / 76–84 from page to page); the config supplies the header. Rendered through the same `_TABLE_SENTINEL` / `tables_by_caption` path; called once from the item branch right after `_swap_layout_text_tables`; a no-op for any reg/row without an entry.
7. **New** `ITEM_FIGURES` + `_item_figures_html` — `<p class="figure-omitted">` placeholders (the `APPENDIX_FIGURES` / eCFR convention) appended to ordinary rows; used for the nine map rows III.F–III.N, each naming its PDF page. Appended in `build_provisions`' second pass to the "heading" and "paras" kinds (`""` for every other row).
8. **New** REG_META flag `heading_line_own_paragraph` + `_inline_heading_stands_alone` — several headings are printed directly above their body with no blank line ("IV. Visibility Standard" / "To be added…", "VI. Carbon Monoxide Standard within the Eisenhower Tunnel* (State Only)" / "Pursuant to…", "V.C.1. Geographic Coverage", "V.A.4.e."–"V.A.4.j."), which `split_into_paragraphs` fused into one paragraph. With the flag, a short un-punctuated label line whose last word is not a function word, followed by an upper-case-initial line and at least one more non-blank line, becomes its own paragraph; a one-line wrapped heading tail ("…Areas in" / "Colorado*", "…for Sulfur" / "Dioxide") or a lower-case sentence wrap ("…primary PM10" / "emissions, including…") is not split. Only `aqs` sets the flag (asserted by test).
9. **New** `AQS_MENTION_RE` + `link_citations` step 1.7 — the document has no regulation number, so `REG_NUM_RE` never sees it. Links "the [Colorado['s]] Ambient Air [Quality] Standards regulation/Rule/Regulation", "Ambient Air Standards [for the State of Colorado] rule", "5 CCR 1001-14" and the printed title to `/regulations/aqs` (`data-provision-id="sec-aqs-top-REG-aqs"`), or to its own root as an `xref` span from inside `aqs`. "National Ambient Air Quality Standards …" is rejected by lookbehind and a keyword after the name is required, so the generic NAAQS phrase never links. **Strict no-op unless `aqs` is in the corpus** (no claim, no bucket entry) — the same corpus gate `_link_cfr49_citations` uses — which is what makes the held-out proof byte-identical.
10. `sources/manifest.json` — `aqs` SOS entry (ruleId 2347, ruleVersionId 12357, effective 2026-01-14) + one `test_freshness.py` test; shipped separately as `regaqs_manifest.patch` (61 freshness tests pass).

New/changed config keys: `CORPUS_REGS["aqs"]`, `REG_META["aqs"]` (+ new flag `heading_line_own_paragraph`), `SOB_SECTION_CONFIG["aqs"]`, `KNOWN_LABEL_FIXES["aqs"]`, `KNOWN_TEXT_FIXES["aqs"]`, `UNCAPTIONED_TABLES["aqs"]`, `COLUMN_LAYOUT_TABLES` (new dict), `ITEM_FIGURES` (new dict), `AQS_MENTION_RE` (new regex). Nothing was added to any existing gated frozenset. Not needed: `SOB_PART_CONFIG`, `FLAT_ENTRY_PART_CONFIG`, `LAYOUT_TEXT_TABLES`, `APPENDIX_*`, `KNOWN_CONTINUATION_LINES`, `REG_CYCLE_AB`, `BARE_DIGIT_CHILD_SECTIONS`, `TABLE_CAPTION_PINS`.

## Label fixes added
Nine consecutive statement-of-basis entries are printed **without the trailing period** every other entry has — confirmed in the PDF text layer with pdfplumber (pages 57–62), not a pdftotext artifact. Without the period none tokenizes, so all nine (~250 lines) were folded into `VIII.P.` and the letter sequence jumped P → Z.

| printed | corrected | source line | pinned by (match_prefix) |
|---|---|---|---|
| `VIII.Q` | `VIII.Q.` | 2288 | "VIII.Q Denver Carbon Monoxide" |
| `VIII.R` | `VIII.R.` | 2319 | "VIII.R Longmont and Colorado Springs Carbon Monoxide" |
| `VIII.S` | `VIII.S.` | 2355 | "VIII.S Denver 8-Hour Ozone" |
| `VIII.T` | `VIII.T.` | 2389 | "VIII.T Denver 8-Hour Ozone" |
| `VIII.U` | `VIII.U.` | 2402 | "VIII.U Denver and Longmont Carbon Monoxide" |
| `VIII.V` | `VIII.V.` | 2438 | "VIII.V Cañon City PM10" |
| `VIII.W` | `VIII.W.` | 2473 | "VIII.W Denver Metro Area/North Front Range 8-Hour Ozone Emissions Budgets" |
| `VIII.X` | `VIII.X.` | 2497 | "VIII.X Pagosa Springs PM10" |
| `VIII.Y` | `VIII.Y.` | 2532 | "VIII.Y Telluride PM10" |

All nine report `OK` (1 hit). The 20 text fixes (all `OK`) are described under change 4.

## Quality-gate results
- **A. Structure — PASS.** The document prints no table of contents; the outline built from its printed headings is I (I.A., I.B., I.B.1.), II Reserved, III (III.A. through III.E. Repealed, III.F.–III.N.), IV, V (V.A., V.A.1.–4., V.A.4.a.–j., V.B. Reserved, V.C., V.C.1.–2., V.D. Repealed), VI, VII (VII.A.–C.), VIII (VIII.A.–Z., AA.–II.). The parsed tree has exactly those sections and children, in order (asserted by `RegAqsFullParseTests.test_structure`). The brief's "V (find it)": V is "V. Emission Budgets for Attainment/Maintenance Areas in the State of Colorado" (line 320, on a page whose whole layout is indented 7 spaces — the ordinary scan accepts it; the heading wraps onto "Colorado"). Section I has **no standards table** in this edition: I.A. only points to 40 CFR Part 50 and I.B.1. is the one prose SO2 standard (the old standards table was removed by VIII.Z., March 18, 2010) — the two real tables are in III and V.A.1. (gate H).
- **B. Coverage — PASS.** 23,373 body words vs 23,502 parsed (100.6%). Front matter dropped by design: the cover page and the incorporation-by-reference notice before "I." (the same treatment every CCR print gets; see "could not resolve" 5).
- **C. Repeated text — PASS.** 0 paragraph prefixes recur ≥ 3× in any row (asserted).
- **D. Giant / fused rows — PASS.** Ten longest: VIII.G 10,522 chars; VIII.Z 8,776; VII.A 8,489; VIII.A 7,608; VIII.D 7,364; VIII.B 6,989; V.A.1 6,856 (the table); VIII.DD 5,799; VIII.M 5,378; VIII.FF 5,356. Every one is a single narrative statement-of-basis entry / the rationale, as printed; nothing fused (before the label fixes VIII.P was a 12,000-char fusion of ten entries — gone).
- **E. Orphans / labels — PASS.** 0 orphans, 0 duplicate ids (asserted). Sequences contiguous except the printed gap III.A → III.F (the line literally reads "III.A. through III.E. Repealed", so `III.A.` exists and B–E do not — no fix invented) and V.A.4.a./b. "Repealed", c./d. "Reserved" (real rows, as printed). 22 marker candidates audited, 6 rejected as continuations — all six correct (five "…established in Section\nV.A.1. shall take effect…" wraps in V.A.4.e.–j. and "…Appendix\nC. Other methods…" in VI, which would otherwise have become a top-level section "C.").
- **F. Statement of basis — PASS.** Section VIII, `SOB_SECTION_CONFIG` (per-section guard, `NO_PART` key); family: ordinary compound `VIII.<letter>` labels — a letter sequence A–Z, AA–II (35 entries) with the topic on the label line and "Adopted: <date>" / "Adopted <date>" as the following paragraph; no opener regex needed. Every entry parsed to its own row in printed order; inner bare-digit lists ("1. Establishing the Primary PM10 Budget") and roman finding lists stay as paragraphs (no compound sub-labels are printed anywhere in VIII — grepped). The Editor's Notes (History) are the tail of VIII.II as `<p>Editor’s Notes History</p><p>Rules V.A.1, …</p>`, the same treatment as Reg 11.
- **G. Cross-references — PASS.** 56 same-document spans (25 self-mentions of "the Ambient Air Quality Standards Regulation/rule" → root; 20 → Section V rows, e.g. "Section V.A.1." in V.A.4.e.–j.; "Section II."/"I.B." in VIII.Z) and 13 external anchors: Reg 3 ×9, Reg 7 ×3, Reg 11 ×1 — all to corpus regs, 0 dead targets (asserted). Buckets: `other_reg` — "Regulation Number 10, Part B" ×5, "Regulation Number 13" ×2, "Regulation 13", "Regulation Number 16" (a Batch 6 key in `agent_small/`; the VIII.M mention will link once merged); `cfr` — "40 CFR Part 93" ×8, "40 CFR Part 58"; `historical` — "Part B" and "IV.D.2.(d)(i)" from the Section III footnote (see "could not resolve" 6); `unparseable` — "V.a.1." (printed typo) and "III.E." (from "III.A. through III.E. Repealed"; III.E. has no row). Corpus grep for the new resolver: "5 CCR 1001-14" appears in **no other source .txt** in this checkout (only its own running header; Reg 3's source is not here); "Ambient Air Quality Standards regulation/Rule" appears in REG_7.txt lines 14742 and 15297 (Part C entries B and G) → **2 new anchors in Reg 7** (proof below); REG_16.txt line 366 "Section V.C.4 of the Colorado Ambient Air Quality Standards Regulation" will link at the root when Reg 16 is parsed; REG_SIP.txt's quoted "“Ambient Air Quality Standards” for the State of Colorado" and ellipsis "Ambient Air Quality Standards... regulation" forms are not matched (reported). Reg 25/24 define ozone areas by their own Appendix A and never name this document (grepped).
- **H. Tables — PASS.** (1) Section III classification table (`sec-aqs-III-A`): 3 columns × 12 body rows including the "PM10" / "Ozone" group rows; every area, classification, effective date and boundary description intact (the Denver Metro/NFR 8-hour ozone lat/long boundary is one cell); the page-4 reprinted header is dropped (one `<th>Area</th>`); the "* The classification…" footnote follows as a paragraph. (2) V.A.1. budgets table (`sec-aqs-V-A-1`): 14 rows — Denver PM10/NOx (with the full trading-provisions text in the budget cell), Denver ozone precursors, six Denver Metro/NFR ozone rows (1997 / 2008 Moderate / 2008 Serious / 2008 Severe / 2015 Moderate / 2015 Serious, each with regional + southern + northern sub-regional NOx/VOC budgets and the "Adopted 20xx, … NAAQS, … SIP" column), Aspen, Cañon City, Lamar, Pagosa Springs, Steamboat Springs (two periods, a page-seam row), Telluride. Every tons/day, tpsd and lbs./day figure is verbatim (asserted for the 2025 Serious SIP row, Steamboat, Telluride and Denver PM10). The header row is config-supplied ("Area / Motor Vehicle Emissions Budget / Adoption") — the source prints none. `tables found in PDF: 2; rendered: 2`.
- **I. Tests — PASS.** `test_import_ccr.py` **395 passed, 7 skipped** (368 + 27 new: `RegAqsMetaTests`, `RegAqsLabelFixTests`, `RegAqsSobSectionTests`, `RegAqsHeadingOwnParagraphTests`, `RegAqsColumnLayoutTableTests`, `RegAqsItemFigureTests`, `RegAqsMentionResolverTests`, `RegAqsFullParseTests`); `test_summarize.py` 161 passed, 2 skipped (+3); `test_freshness.py` 61 passed (+1).

## No-op proofs
Baselines were built FIRST with `import_ccr.ORIGINAL.py`, one parse per subprocess, sequentially (`out/base_26.json`, `base_7.json`, `base_30.json`, `base_ecmc.json`, `base_25.json`; runner log `out/base_runner.log`). `prove_aqs_batch6.py <key> with|without` re-parses each with the edited importer through `cmd_parse` (so the JSON is written identically), with `aqs` deleted from `CORPUS_REGS` at runtime or with the full corpus, one process at a time (Reg 7 "with" needed five attempts — the box was at 6.5 GB with two other agents' ECMC parses running; `out/noop_aqs_runner*.log`); `prove_aqs_batch6.py check` compares (`out/noop_aqs/prove.json`, "ALL PROOFS PASS"):
1. **`aqs` removed from the corpus:** `cmp` **byte-identical** to the ORIGINAL baseline for all five (26 / 7 / 30 / ecmc / 25 — parsed JSON; the `_unresolved.json` bucket files are identical too).
2. **`aqs` in the corpus:** 26, 30, ECMC and 25 are still `cmp`-byte-identical to their baselines (0 anchors — none of them names this document). **Reg 7 changes in exactly 2 rows by exactly 2 new anchors**, both `<a class="xref-external-reg" data-provision-id="sec-aqs-top-REG-aqs" href="/regulations/aqs">…</a>`: `sec-7-C-B` — "…together with the relevant amendments to the **Ambient Air Quality Standards regulation** and Regulation Number 3" (REG_7.txt line 14742); `sec-7-C-G` — "…which is defined in the **Ambient Air Quality Standards Rule**" (line 15297). Stripping the two anchors reconstructs the baseline exactly, field by field (0 unexplained rows); the existing "Regulation Number 3" anchor next to the first one is unchanged. Only five anchors were asked for; there are only two in the whole existing corpus.

## Deliverables (all in `agent_aqs/`)
`out/regaqs_parsed.json` (+ `_unresolved` / `_duplicate_ids` / `_corrections` / `_marker_audit.json`), `out/regaqs_db.json` (`[]`), `out/regaqs_diff_report.md` (80 only-parsed, 0 DB), `out/apply_regaqs/` (plan.json, stats.md — all three sanity checks PASS, 80 new / 0 changed / 0 obsolete, two upsert SQL files), `regaqs.patch` (import_ccr + test_import_ccr vs the ORIGINALs; applies cleanly with `patch -p0 --dry-run`), `regaqs_summarize.patch` (summarize + test_summarize — the shipped `*.ORIGINAL.py` copies were stale, so `summarize.py` / `test_summarize.py` were copied over them first, as the brief says, and the patch is against those), `regaqs_manifest.patch` (manifest + test_freshness), `prove_aqs_batch6.py` + `out/noop_aqs/` + `out/base_*.json`, this `REPORT.md`.

## Summarizer (`regaqs_summarize.patch`)
- `REG_AUDIENCE["aqs"]` = "an air-quality planner or permit engineer checking Colorado's ambient standards, nonattainment/maintenance area designations and motor vehicle emissions budgets" (the O&G default is gone from this prompt — asserted).
- `REG_PROMPT_HINTS["aqs"]` — 193 words. Baked in: not a numbered regulation; "Commission" = AQCC, "Division" = APCD; I.A only points to 40 CFR Part 50; the I.B SO2, IV visibility and VI Eisenhower Tunnel CO standards are **State Only** — say so and quote levels / averaging times / units exactly as printed, never converted (700 micrograms per cubic meter three-hour maximum; .076/km, four hours; 100 parts per million, 15 minute average); Section III designations, effective dates and boundaries only as printed, no county or date the row omits, map rows are headings whose map is not reproduced; Section V budgets (tons/day, tpsd, lbs./day, years) verbatim, naming a budget's NAAQS/SIP only when the row does; Repealed/Reserved rows have no content; `[1]`–`[5]` are footnote markers; VII is rationale and VIII rulemaking history, not current requirements. Tests: selected by id prefix for three ids, required markers present, ≤ 200 words, never leaks into Reg 7 / 1 / cp / 25 / ECMC prompts; `test_only_reg_11_overrides_the_audience` now expects `{"11", "12", "25", "27", "aqs"}` (the other Batch 6 agents will add their keys to that same set on merge).

## Things I could not resolve
1. **`sec-aqs-III-A`** — the printed line is "III.A. through III.E. Repealed", so the row's citation is `III.A.`, its first paragraph "through III.E. Repealed" (the diff report's one lower-case-start row), and — because the Section III table is printed immediately after that line — the classification table and its footnote live in this row rather than in `sec-aqs-III`. Faithful to the print; a synthetic "III.A.–III.E." row or moving the table to the section row would invent structure.
2. **"Attainment/Mainte nance"** in the Section III table (Denver 1-Hour Ozone row, Area cell): the PDF breaks the word across two lines inside the cell with no hyphen and pdfplumber returns "Attainment/Mainte\nnance", rendered with a space. One cell; no mechanism edits pdfplumber cell text and I did not add one.
3. **V.A.1. table header** is config-supplied ("Area / Motor Vehicle Emissions Budget / Adoption"); the source prints none. The Denver PM10 budget cell (trading provisions, ~300 words) is one cell with its inner "1) … (2) … (3) …" list inline — as printed in that cell.
4. **Printed typo "Section V.a.1."** (V.A.4.j., line 606) — left as printed (`unparseable` bucket, 1 mention); the sibling rows cite "Section V.A.1." and link. A one-line `KNOWN_TEXT_FIXES` entry would correct it if the CEO prefers.
5. **Front-matter incorporation-by-reference notice** (lines 24-36; "…any incorporation by reference of 40 CFR Section 93.118 are to the edition published March 14, 2012") is dropped with the cover page, exactly as for every other CCR print (Reg 1/cp/9 drop theirs the same way). Flagged because for this document that sentence pins the 40 CFR 93.118 edition the V.A.4. effective-date rows depend on.
6. **Section III footnote cite** "Regulations Number 1, 5 CCR 1001-3, Section VIII; or Number 3, 5 CCR 1001-5, Part B, Section IV.D.2.(d)(i) or (ii)" — plural "Regulations Number 1 … Number 3" is not a `REG_NUM_RE` shape, so neither Reg 1 nor Reg 3 links there and "Part B" / "IV.D.2.(d)(i)" land in the historical bucket. One paragraph.
7. **Resolver coverage:** the lower-case "the ambient air quality standards regulation" (VIII.L., line 2147) and the SIP's quoted / ellipsis forms are deliberately not matched (case-sensitive name + required keyword, so the generic NAAQS phrase can never link). Reg 16's "Section V.C.4 of the Colorado Ambient Air Quality Standards Regulation" will link at the root only; the "Section V.C.4" before the name is not deep-linked (no "Section X of <name>" shape exists for Common Provisions either).
8. **"Bray"** in IV's footnote 2 ("Where Bray is the Rayleigh scattering coefficient") is the PDF's own text (presumably B_ray) — left as printed.
9. Section VI's "* Carbon Monoxide/Eisenhower Tunnel: Adopted: 9/5/75 Effective: 12/17/75" footnote is printed with no blank line after the standard's sentence and so ends that paragraph; the "1."–"4." Method of Testing items are body paragraphs (bare digits never tokenize under CYCLE_AB), items 1–3 in one paragraph as printed (no blank lines between them in the source).
10. Wrapped section headings ("III. … in / Colorado*", "V. … State of / Colorado", "VII.A. … for Sulfur / Dioxide", "V.C. … Attainment/Maintenance / Area") follow the existing convention for every other reg: the row's title is the bare citation and the heading is its first `<p>`.

## Anything the summarizer should be warned about
- **Not a numbered regulation** — never "Regulation Number 14"; "5 CCR 1001-14" is the cite. "Commission" = AQCC; "Division" = Air Pollution Control Division; "MPO", "RAQC", "DRCOG", "PPACG", "TIP", "SIP" appear undefined — expand only as the text does.
- **State Only:** I.B (Colorado SO2 standard: 700 µg/m³ three-hour maximum, 0.267 ppmv, no more than once in any twelve-month period) and VI (Eisenhower Tunnel CO: 15 minute average of 100 ppm / 115 mg/m³) are State Only as printed; IV (visibility: extinction .076/km, standard visual range 32 miles, four contiguous hours, 0800–1600, RH < 70 %) applies in the AIR program area only. I.A is only a pointer to 40 CFR Part 50 — **no NAAQS levels are printed anywhere in this document**; never supply them.
- **Area designations and dates only as printed**: the Section III table's effective dates (10/16/02, 11/24/04, 8/14/01, 7/14/03, 7/31/00, 11/25/05, 10/11/01, 11/20/07), classifications ("Attainment/Maintenance", "Nonattainment"), and boundaries ("See attached map." for six areas — the map is a placeholder). III.B–E are repealed; III.F–N are map headings only.
- **Budgets**: quote tons/day, "tpsd" (= tons per summer day, defined in the table), lbs./day and years verbatim; the V.A.1. rows for the Denver Metro/NFR 8-hour ozone area exist for several NAAQS/SIP stages and two of them are "(State Only)"; V.A.4.e.–j. give an interim budget that applies "until" the federal adequacy finding — keep both numbers and the condition; "Repealed"/"Reserved" rows (II., V.A.4.a.–d., V.B., V.D.) have no content.
- `[1]`–`[5]` in I.B.1. and IV are footnote markers added for readability (the print uses superscripts); "*" footnotes (III heading, VI, IV's adoption line) are as printed.
- VII (Rationale — the 1970s SO2 standard discussion) and VIII (35 statements of basis, 1995–2025, including repealed CO areas, old 1-hour ozone budgets and superseded PM10 budgets) are history — nothing in them is a current requirement; several VIII entries cite Section V.B./V.D. content that is now Reserved/Repealed.
