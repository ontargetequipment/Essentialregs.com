# Batch 4 merge notes — APCD General Permits (gp01–gp12) + 40 CFR 60 JJJJ/IIII + 40 CFR 63 ZZZZ (Sept 19, 2026)

- `import_ccr.py` = the general-permits agent's importer + the federal agent's touchpoint patch (`ECFR_REGS` dispatch set replacing `startswith("ooo")`, `CORPUS_REGS`/`CFR_SUBPART_TO_REGKEY`/`REG_META` for jjjj/iiii/zzzz) + a CEO addition: `PROGRAM_SUBPART_RE` / link_citations step 1.2 so "NSPS Subpart IIII" / "NESHAP Subpart ZZZZ" (no "40 CFR Part" prefix) link to the corpus subparts. GP09/GP10 root titles carry "(attainment areas)"/"(nonattainment areas)"; GP12's root title normalized to "GENERAL PERMIT 12 (GP12) — Well Production Facilities — GP12 Issuance 1, May 28, 2026".
- `import_ecfr.py` generalized to `SUBPART_META` (six subparts); tables for jjjj/iiii/zzzz come from the eCFR XML (`pipeline/sources/<CODE>.xml`, resolved as the PDF path with `.xml`); OOOOa/b/c byte-identical to their baselines.
- Tests: 260 importer (CCR + eCFR) + 103 summarizer pass; `CrossRefNoOpProofTests.NEW_REGS` extended to every post-baseline reg.
- Proof: Reg 1/2 byte-identical; Reg 26 gains 6 links, Reg 30 13 links (to JJJJ/IIII/ZZZZ), GP permits gain 20 links to the federal subparts; every difference is a link, nothing else. Four new regs parsed via the merged importer are identical to the agents' outputs except those links.
- Row counts: gp01 103, gp02 210, gp03 59, gp05 111, gp06 174, gp07 124, gp08 119, gp09 251, gp10 254, gp11 129, gp12 540 (2,074; ~1,201 summary-eligible); jjjj 205, iiii 288, zzzz 360 (853; 548 summary-eligible).
- Workflow: `.github/workflows/import.yml` maps gp01–gp12 and jjjj/iiii/zzzz to upper-cased basenames — the updated file is delivered as `docs/imports/2026-09-19/import.yml.new` for Claude Code to copy into place (the device bridge cannot write `.github`).
